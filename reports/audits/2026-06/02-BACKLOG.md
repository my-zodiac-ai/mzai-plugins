# mzai-plugins — Бэклог улучшений + дорожная карта

> **Дата:** 2026-06-28
> **Основано на:** `01-CRITIQUE.md` (находки C1…M6), `03-UNIVERSAL-STANDARD.md` (правила S/P/H/D/SDD/CI), `04-ENGINEERING-CANON.md`.
> **Принцип приоритизации:** сначала «починить сломанное» (корректность/безопасность), потом «фундамент портируемости», потом «дедуп», потом «расширение». Внутри — по `value × reach`.
> **reach** = в скольких из 3 проектов орги (my_zodiac_ai / companion-ai / astro-ai-landing) даёт пользу.

> **ОБНОВЛЕНО 2026-06-28:** `speckit` и `speckit-product-forge` вынесены из репо (внешние SpecKit-инструменты: github/spec-kit + VaiYav/speckit-product-forge). Поэтому задачи про их внутренности — **B-02, B-08, B-11 — СНЯТЫ** (это апстрим, не наш код). SDD-конвеншн (`constitution.md` + `specs/<NNN-slug>/`) остаётся как соглашение об использовании внешних инструментов на проектах (B-15). Скоуп репо — теперь 7 плагинов.

## Сделано в 2026-06 (Wave 0 + Wave 1) ✅

**Wave 0 (починка):**
- ✅ **B-01** — баг чтения stdin в хуках исправлен (`lib/_common.sh`: читаем stdin один раз, payload аргументом); `gsap`/`i18n` ожили; добавлены позитивные smoke-тесты → 30/30; убрано ложное заявление в README хуков.
- ✅ **B-03** — закрыт удалением speckit (его `qa-*.sh` с хардкодом пути ушли вместе с плагином).
- ✅ **B-04** — инфра-литералы → плейсхолдеры (`${NEW_RELIC_ACCOUNT_ID}`, `${NEW_RELIC_BROWSER_APP_ID}`, `mcp__posthog`), убраны `/Users//sessions` пути. Grep-гейт чист.
- ✅ **B-05** — `install.sh` переписан: бэкап `.bak` перед перезаписью, установка во все клиенты, Bash 3.2-совместимость.
- ✅ **B-10** — `scripts/lint-plugins.py` (CI-1…CI-7) + `.github/workflows/lint-plugins.yml`. Зелёный: 0 ошибок.

**Wave 1 (фундамент):**
- ✅ **B-06/B-07** — введена модель **core + adapter**. `zodiac-dev-toolkit` распущен на: `core-engineering` (7), `ai-llm` (2), `adapter-nestjs` (6, +новый `nestjs-caching`), `adapter-vue-quasar` (5), `observability` (2), `domain-astrology` (2). Агенты перенесены. Marketplace: 12 плагинов, manifest == каталоги.
- ✅ **De-domain core** — `caching-patterns` (новый), `testing-patterns`, `api-changelog`, `docker-compose-ops`, `load-testing` переписаны как стек-агностичные; проектные примеры — под `## Example`. CI-7 по `core-*` чист (0 предупреждений).
- ✅ **B-09** — README/marketplace/версии приведены в порядок; счётчики совпадают (CI-4 зелёный).

**Wave 2 (дедуп + расширение):**
- ✅ **B-12** — 12 зеркальных агентов (quality-gate + design-review) → тонкие shim'ы (Glob+Read парный скилл = единый источник правды); уникальные цвета.
- ✅ **B-14** — `architecture-audit` расцеплён от проектных ADR/dep-graph (универсальное → Engineering Canon; проектное → overlay/пример); README quality-gate выправлен.
- ✅ **B-18** — новые `adapter-nuxt` (4 скилла) и `adapter-prisma` (3 скилла) для companion-ai / astro-ai-landing. Теперь **14 плагинов**.
- ✅ **B-13** — `zodiac-research-lab` помечен `domain:astrology` + условный deferral к хостовым скиллам (без жёсткой зависимости).
- ✅ **Polish** — `allowed-tools` на 14 скиллах (+2 MCP-скилла исключены в линтере), top-level **LICENSE** (MIT + ссылка на Apache cowork). **Линтер: 0 ошибок, 0 warnings.**

**Остаётся (опционально / в других репо):** B-15 (реальный `constitution.md` на проект — в продуктовых репо), B-16 (вынести `astrology-data-validator` в продуктовый репо + провенанс данных из JPL Horizons), B-17 (решить vendoring `cowork-plugin-management`), точечный scoping `Bash(<cmd>:*)` в `allowed-tools` (сейчас задекларирован общий набор), B-14-full (остальные quality-gate скиллы). Push в origin — за вами.

---

## Что уже сделано в 2026-05 (не переоткрывать, но доделать)
- ✅ Создан `zodiac-hooks-pack` — **но** 2 хука мертвы (баг stdin) → см. B-01.
- ✅ Поднято 12 скиллов в `zodiac-dev-toolkit` (11→23) — **но** без параметризации, с хардкодом путей/ID → см. B-04, B-07.
- ◑ Частичный speckit-дедуп (внешние репо) — вне этого репозитория, не проверяемо здесь.
- ❌ README/метаданные так и не обновлены под новое состояние → B-09.

---

## P0 — Stop the bleeding (сломано / небезопасно). Цель: 3–5 дней

| ID | Задача | Находка | value×reach | Усилие | Зависимости |
|---|---|---|---|---|---|
| **B-01** | Починить баг чтения stdin в хуках; оживить `gsap-import-guard` и `i18n-key-checker`; добавить позитивные smoke-тесты | C3, H1/H8 | 5×3 | 0.5д | — |
| ~~**B-02**~~ | ~~Поставить `speckit-product-forge/docs/`~~ — **СНЯТО**: forge вынесен (внешний VaiYav/speckit-product-forge); это апстрим-баг, не наш | C2 | — | — | — |
| **B-03** | Убрать хардкод `/Users/valentinyakovlev/...` из `qa-check/fix/report.sh`; удалить скрипты-сироты | C4 | 4×3 | 0.25д | — |
| **B-04** | Зачистить инфра-литералы и абсолютные/`/sessions/` пути по всему репо; завести grep-гейт | C1, M2, H6 | 5×3 | 1д | B-10 (гейт) |
| **B-05** | Исправить `install.sh`: бэкап перед `rm -rf`, оба клиента, guard на Bash≥4 | M3 | 4×3 | 0.5д | — |

**Acceptance P0:**
- B-01: `printf '<qualifying payload>' | hook` печатает предупреждение; `tests/smoke.sh` содержит позитивный кейс на каждый advisory-хук; `bash -n` зелёный.
- B-02: `ls speckit-product-forge/docs` существует; CI-1 не находит висячих `../docs/*`.
- B-03: `grep -rn '/Users/' plugins/` → 0; ни один SKILL не ссылается на удалённые скрипты.
- B-04: `grep -rnE '/Users/|/sessions/|7715788|538822289|mcp__[0-9a-f]{8}' plugins/` → 0.
- B-05: повторный `install.sh` не теряет чужой одноимённый плагин; ставится в оба обнаруженных клиента.

---

## P1 — Фундамент портируемости + достоверность. Цель: 2 недели

| ID | Задача | Находка/Правило | value×reach | Усилие | Зависимости |
|---|---|---|---|---|---|
| **B-06** | Ввести модель **universal-core + thin adapter**; создать каркас `core-engineering` | S-1…S-4 | 5×3 | 1д | — |
| **B-07** | Разбить `zodiac-dev-toolkit` (грабь-бэг) → `core-engineering` + `adapter-nestjs` + `adapter-vue-quasar` + `observability` + `ai-llm` + `domain-astrology` | P-6, C1 | 5×3 | 4–6д | B-06 |
| ~~**B-08**~~ | ~~Расцепить `speckit` шаблоны/ядро~~ — **СНЯТО**: speckit вынесен (внешний github/spec-kit). Если нужна де-связка — это PR в апстрим/форк, не в наш репо | H1, H2 | — | — | — |
| **B-09** | Привести в порядок все метаданные/версии/README; удалить/перегенерить `BUNDLING_REPORT.md` | D-1…D-4, M1, M2, H6 | 4×3 | 1д | B-10 |
| **B-10** | **Plugin CI-линтер** (CI-1…CI-7) — мультипликатор: ловит битые ссылки, хардкод, рассинхрон счётчиков | CI-* | 5×3 | 2–3д | — |
| ~~**B-11**~~ | ~~Разрулить двойную проводку v-model~~ — **СНЯТО**: v-model живёт в внешних speckit/forge; апстрим-вопрос | H3 | — | — | — |

**Acceptance P1:**
- B-06/B-07: каждый скилл имеет `x-scope`; `core-*` проходит CI-7 (нет доменных/стек-терминов вне `## Example`); адаптеры ссылаются на core, не копируют метод.
- B-08: `templates/*` стек-нейтральны; один feature-dir конвеншн; `forge` на рантайме не тянет захардкоженный `back/`/`front/`.
- B-09: счётчики README == `find`; одна версия на плагин; 0 битых путей в доках.
- B-10: CI падает на синтетическом PR с битой ссылкой и с захардкоженным account ID.
- B-11: ровно одна рабочая ветка v-model; RTM генерируется или v-model явно помечен внешней зависимостью.

---

## P2 — Дедупликация и консолидация. Цель: 1 неделя

| ID | Задача | Находка/Правило | value×reach | Усилие | Зависимости |
|---|---|---|---|---|---|
| **B-12** | Схлопнуть дубли «скилл↔агент» в `quality-gate` (7) и `design-review` (5): агенты → чистые shim'ы «Read skill X, apply» | H4, A-1/A-2 | 4×3 | 1–2д | — |
| **B-13** | Заменить `zodiac-research-lab` тонкими обёртками над хостовыми скиллами, либо удалить | H5, A-4 | 3×2 | 1д | — |
| **B-14** | Переписать `quality-gate` на ссылки в Engineering Canon вместо проектных ADR; проектные правила → overlay | canon §1–§7 | 4×3 | 2д | B-06 |
| **B-15** | Поставить реальный `constitution.md` на проект (не только шаблон) | SDD-1 | 4×3 | 0.5д/проект | B-08 |

**Acceptance P2:**
- B-12: агент не содержит методологии, дублирующей скилл; в параллельном наборе у каждого уникальный `color`.
- B-13: нет 6/6 одноимённых дублей; остаётся максимум тонкий астро-контекст поверх хостового `deep-research`.
- B-14: `architecture-audit`/`code-quality-audit`/`testing-audit`/`security-audit` цитируют canon; ADR-NNN отсутствуют в телах core-скиллов.

---

## P3 — Размещение домена и расширение. Цель: по возможности

| ID | Задача | Находка/Правило | value×reach | Усилие | Зависимости |
|---|---|---|---|---|---|
| **B-16** | Перенести `astrology-data-validator` в продуктовый репо как internal-tool; починить провенанс reference-данных (JPL Horizons/astronomy-engine, пер-значно) | M6, D-5 | 3×1 | 1–2д | — |
| **B-17** | Решить судьбу `cowork-plugin-management`: ссылаться upstream или добавить top-level LICENSE + `keywords` | M4, P-5 | 2×3 | 0.25д | — |
| **B-18** | Адаптеры под не-Quasar стеки: `adapter-nuxt`, `adapter-prisma` (закрывает разрыв 2026-05 для companion-ai / astro-ai-landing) | прошлый gap-report | 4×2 | 3–5д | B-06, B-07 |

**Acceptance P3:**
- B-16: каждое reference-значение имеет source+JD+version+date; плагин не в общем marketplace.
- B-18: companion-ai (Nuxt+Prisma) и astro-ai-landing (Nuxt4) используют core + свой adapter без правок core.

---

## Дорожная карта (волнами)

**Волна 0 — «Починить» (P0, 3–5 дней).** B-01, B-03, B-04, B-05 (B-02 снято) + завести B-10 пораньше (гейт нужен для B-04). После: ничего мёртвого/опасного, нет утечек путей.

**Волна 1 — «Фундамент» (P1, ~2 недели).** B-06 → B-07 (B-08/B-11 сняты — speckit/forge внешние), B-09 после B-10. После: есть модель core+adapter, `dev-toolkit` разбит, метаданные честные, CI ловит регрессии.

**Волна 2 — «Дедуп» (P2, ~1 неделя).** B-12…B-15. После: нет дублей скилл↔агент и research-рескина; quality-gate на каноне; у проектов реальная конституция.

**Волна 3 — «Расширение» (P3).** B-16…B-18. После: домен вынесен, появились адаптеры под Nuxt/Prisma, тулкит реально универсален для всей орги.

---

## Карта «находка → задача» (для трассировки)

| Находка (01-CRITIQUE) | Задача |
|---|---|
| C1 инфра-литералы | B-04, B-07 |
| C2 нет forge/docs | B-02 |
| C3 мёртвые хуки | B-01 |
| C4 хардкод пути в qa-* | B-03 |
| H1 speckit-шаблоны coupling | B-08 |
| H2 две модели каталогов | B-08 |
| H3 v-model двойная проводка | B-11 |
| H4 дубли скилл↔агент | B-12 |
| H5 research-lab рескин | B-13 |
| H6 враньё в README/доках | B-09 |
| M1 рассинхрон версий | B-09 |
| M2 BUNDLING_REPORT устарел | B-09 |
| M3 install.sh опасен | B-05 |
| M4 vendored cowork | B-17 |
| M5 frontmatter/author | B-09, B-12 |
| M6 провенанс данных | B-16 |
| (нет single source of truth/CI) | B-10 |
| (нет core/adapter границы) | B-06, B-07 |

---

## Метрика готовности тулкита («universal-ready»)
1. `core-*` плагины проходят CI-7 (0 доменных/стек-терминов вне `## Example`).
2. Любой скилл соответствует «Definition of Done» из `03-UNIVERSAL-STANDARD.md` §9.
3. companion-ai и astro-ai-landing подключают marketplace и реально используют ≥5 core-скиллов без правок.
4. CI-линтер зелёный на каждом PR; счётчики README автогенерируются.
5. Один SDD-конвеншн (`constitution.md` + `specs/<NNN-slug>/`) применён хотя бы на одном проекте целиком.
